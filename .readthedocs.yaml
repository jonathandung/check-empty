version: 2
sphinx:
  configuration: docs/source/conf.py
build:
  os: ubuntu-26.04
  jobs:
    post_create_environment: [python3 -m pip install -e . --group docs]
  tools:
    python: latest
formats: [htmlzip, pdf]
